# Buildable examples. Kept out of the default driver set (drivers/) because they
# ship no engine support — they exist to be read and copied, and to prove the
# public driver SDK compiles a fresh plugin under the project's warnings.
add_subdirectory(driver-template)
